var app = angular.module('myApp',[]);
app.controller('MainController', ['$scope', 'DemoFactory', function ($scope, DemoFactory) {
	$scope.description = DemoFactory;
}]);
app.controller('SectionController', ['$scope', 'DemoFactory', function ($scope, DemoFactory) {
	$scope.description = DemoFactory;
}]);